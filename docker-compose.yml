services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - '8000:8000'

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    ports:
      - '80:80'
    depends_on:
      - backend
